# Managed by Ansible — do not edit manually
# Host: {{ inventory_hostname }}

guid=
zone=
pod=
cluster=
host={{ wg_ip }}
cluster.storage.operation.timeout=300
host.storage.pool.id=

# Management server connection
mgt.server.ip={{ cs_management_host }}
mgt.hostPort={{ cs_management_port }}

# KVM settings
hypervisor.type=kvm
guest.cpu.mode=host-passthrough
network.bridge.type=native

# Storage type for this host's cluster
{% if inventory_hostname in groups['nfs_cluster'] %}
local.storage.uuid=
{% endif %}
